{
  "name": "@drarig29/modular-cli.plugin-foo",
  "version": "0.0.3",
  "main": "index.js",
  "scripts": {
    "build": "esbuild --platform=node --bundle index.js --minify --outdir=dist"
  },
  "devDependencies": {
    "esbuild": "^0.20.1"
  }
}